"use client";

// Converting Mantine to Client Components
import {
  TextInput,
  Title as Typography,
  Input,
  Textarea,
  NumberInput,
  Group,
} from "@mantine/core";

// Switching to Material Tailwind if Mantine is not good
import { Button } from "@material-tailwind/react";

export { TextInput, Typography, Button, Input, Textarea, NumberInput, Group };
